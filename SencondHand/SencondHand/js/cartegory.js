const itemsliderbar = document.querySelectorAll(".cartegory-left-li");
itemsliderbar.forEach(function (nemu, index) {
  menubar.addEventListener("Click", function () {
    menubar.classList.toggle("block");
  });
});
